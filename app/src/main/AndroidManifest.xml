<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.magnify.utils">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.magnify.utils.base.GlideConfiguration"
            android:value="GlideModule" />

        <activity
            android:name=".ui.MainActivity"
            android:label="工具类实用集锦">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".ui.RandCharActivity" />
        <activity android:name=".ui.SaveToolAndPreferenceActivity" />
        <activity android:name=".ui.CreateDataUtilsActivity" />
        <activity android:name=".ui.CreateDetailActivity" />
    </application>

</manifest>